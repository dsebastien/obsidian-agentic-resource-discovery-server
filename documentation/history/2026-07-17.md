# 2026-07-17

## Done

- Fixed standing CI failure at `format:check`: ran `bun run format` repo-wide (15 files: docs/usage.md, docs/configuration.md, documentation/Architecture.md, documentation/Configuration.md, 11 `src/app/**` files). Verified `format:check`, `tsc`, `lint`, `bun test` (214 pass), `build` all green. Commit `b657f68` (`style(all): format repository with prettier`). CI run 29563257925 green.
- Cut release **0.0.2** via the new two-phase release workflow (`gh workflow run release.yml -f version=0.0.2`):
    - Prepare run 29563325374 (main): version bump, changelog, tag `0.0.2` at commit `ee4d803`, dispatched publish at the tag ref.
    - Publish run 29563375472 (ref `0.0.2`): built, attested, released with assets `main.js`, `manifest.json`, `styles.css`.
- Verified attestation: `gh attestation verify` on released `main.js` — certificate `sourceRepositoryDigest` = `ee4d80399da819be0fdfc1447a94ff925232c08f` = `git rev-parse 0.0.2`. The two-phase workflow now binds attestations to the tag commit (0.0.1's attestation was bound to a main commit instead).

## Notes

- `gh attestation verify` returns two matching attestations for `main.js`: the new one at `refs/tags/0.0.2` and the legacy 0.0.1 one at `refs/heads/main` (commit `24ff55c`) — the bundle digest happened to be identical across both builds. The tag-bound attestation is the authoritative one.

## Open questions / blockers

- None.
